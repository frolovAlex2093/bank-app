spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            provider: keycloak
            client-id: bank-front-client
            client-secret: test-secret
            scope: openid, profile
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/keycloak"
        provider:
          keycloak:
            # issuer-uri не указываем – вместо этого задаём все endpoint'ы явно
            authorization-uri: http://localhost:9090/realms/bank-realm/protocol/openid-connect/auth
            token-uri: http://localhost:9090/realms/bank-realm/protocol/openid-connect/token
            jwk-set-uri: http://localhost:9090/realms/bank-realm/protocol/openid-connect/certs
            user-name-attribute: preferred_username   # если используется
  # Остальные настройки для тестов (например, отключение Eureka)
  cloud:
    config:
      enabled: false
eureka:
  client:
    enabled: false